# models/product_product.py
from odoo import models

class ProductProduct(models.Model):
    _inherit = 'product.product'

    # Tidak ada perubahan logika di sini, hanya untuk memastikan model terhubung
    pass