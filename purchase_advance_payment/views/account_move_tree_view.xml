<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_move_tree_inherit_commercial_invoice" model="ir.ui.view">
        <field name="name">account.move.tree.inherit.commercial.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_tree"/>
        <field name="arch" type="xml">

            <!-- Tambahkan badge status di awal kolom -->
            <xpath expr="//field[@name='state']" position="before">
                <field name="state"
                       widget="badge"
                       decoration-info="state == 'commercial_invoice'"
                       decoration-success="state == 'posted'"
                       decoration-muted="state == 'draft'"
                       decoration-danger="state == 'cancel'"
                       options="{'clickable': false}"
                       />
            </xpath>

        </field>
    </record>
</odoo>