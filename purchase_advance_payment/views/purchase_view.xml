<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
<!--             <xpath expr="//header" position="inside">
                <button
                    name="%(action_view_account_voucher_wizard)d"
                    string="Pay Advance via Action"
                    type="action"
                    class="btn-primary"
                    groups="account.group_account_invoice"
                    attrs="{'invisible': ['|', ('state', 'in', ['done', 'cancel']), ('amount_residual', '=', 0)]}"
                />
            </xpath> -->
            <button name="button_draft" position="after">
                <button
                    name="%(action_view_account_voucher_wizard)d"
                    string="Pay TT"
                    type="action"
                    class="btn-primary"
                    groups="account.group_account_invoice"
                    attrs="{'invisible': ['|',('state', 'in', ['done','cancel']),('amount_residual', '=', 0)]}"
                />
            </button>
            <notebook position="inside">
                <page string="Payment advances" groups="account.group_account_invoice">
                    <field
                        name="account_payment_ids"
                        nolabel="1"
                        colspan="4"
                        context="{'form_view_ref': 'account.view_account_payment_form','tree_view_ref': 'account.view_account_payment_tree'}"
                    />
                </page>
            </notebook>
            <field name="amount_total" position="after">
                <div class="oe_subtotal_footer_separator oe_inline o_td_label">
                    <label for="amount_residual" />
                </div>
                <field
                    name="amount_residual"
                    nolabel="1"
                    class="oe_subtotal_footer_separator"
                    widget="monetary"
                    options="{'currency_field': 'currency_id'}"
                />
            </field>
            <field name="amount_total" position="after">
                <field name="payment_progress"
                       widget="badge"
                       decoration-success="payment_progress == 'full_payment'"
                       decoration-info="payment_progress == 'balance_payment'"
                       decoration-warning="payment_progress == 'commercial_invoice'"
                       decoration-danger="payment_progress == 'down_payment'" />
            </field>
        </field>
    </record>

    <record id="action_purchase_advance_payment" model="ir.actions.act_window">
        <field name="name">Pay Advance</field>
        <field name="res_model">account.voucher.wizard.purchase</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_view_types">form</field>
        <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
    </record>

    <record id="purchase_order_view_tree" model="ir.ui.view">
        <field name="name">purchase.order.view.tree</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_view_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field
                    name="advance_payment_status"
                    decoration-success="advance_payment_status == 'paid'"
                    decoration-info="advance_payment_status == 'partial'"
                    decoration-warning="advance_payment_status == 'not_paid'"
                    widget="badge"
                    optional="hide"
                />
            </field>
            <field name="amount_total" position="after">
                <field
                    name="amount_residual"
                    sum="Amount Residual"
                    widget="monetary"
                    decoration-bf="1"
                    optional="hide"
                />
            </field>
            <field name="partner_id" position="after">
                <field name="payment_progress"
                       widget="badge"
                       decoration-success="payment_progress == 'full_payment'"
                       decoration-info="payment_progress == 'balance_payment'"
                       decoration-warning="payment_progress == 'commercial_invoice'"
                       decoration-danger="payment_progress == 'down_payment'"
                       optional="hide"/>
            </field>
        </field>
    </record>

    <record id="purchase_order_kpis_tree" model="ir.ui.view">
        <field name="name">purchase.order.inherit.purchase.order.tree</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field
                    name="advance_payment_status"
                    decoration-success="advance_payment_status == 'paid'"
                    decoration-info="advance_payment_status == 'partial'"
                    decoration-warning="advance_payment_status == 'not_paid'"
                    widget="badge"
                    optional="hide"
                />
            </field>
            <field name="amount_total" position="after">
                <field
                    name="amount_residual"
                    sum="Amount Residual"
                    widget="monetary"
                    decoration-bf="1"
                    optional="hide"
                />
            </field>
        </field>
    </record>

    <record id="view_account_payment_tree_inherit" model="ir.ui.view">
        <field name="name">account.payment.tree.inherit.purchase.advance</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount']" position="before">
                <field name="percentage_advance" string="Advance (%)"/>
            </xpath>
        </field>
    </record>

</odoo>