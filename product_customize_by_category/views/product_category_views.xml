<odoo>
    <record id="view_product_category_form_inherit" model="ir.ui.view">
        <field name="name">product.category.form.inherit</field>
        <field name="model">product.category</field>
        <field name="inherit_id" ref="product.product_category_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/group" position="after">
                <group string="Categories Template">
                    <field name="spec_field_ids" context="{'default_field_type': 'spec'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <!-- <field name="motor_field_ids" context="{'default_field_type': 'motor'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name" default="Motor Type"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="bearing_field_ids" context="{'default_field_type': 'bearing'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="kss_field_ids" context="{'default_field_type': 'kss'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="cablespeed_field_ids" context="{'default_field_type': 'cablespeed'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="remote_field_ids" context="{'default_field_type': 'remote'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="tou_field_ids" context="{'default_field_type': 'tou'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="led_field_ids" context="{'default_field_type': 'led'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="book_field_ids" context="{'default_field_type': 'book'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field> -->

                    <field name="material_field_ids" context="{'default_field_type': 'material'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="cable_field_ids" context="{'default_field_type': 'cable'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

                    <field name="color_field_ids" context="{'default_field_type': 'color'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field>

<!--                     <field name="packing_field_ids" context="{'default_field_type': 'packing'}">
                        <tree editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="field_type" invisible="1"/>
                        </tree>
                    </field> -->
                </group>
            </xpath>
        </field>
    </record>
</odoo>