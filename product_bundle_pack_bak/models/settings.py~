from odoo import fields, models


class ResConfigSettings(models.TransientModel):
    _inherit = 'res.config.settings'

    allow_bundle = fields.Boolean(
        string="Allow Manual Dropshipping Delivery",
        config_parameter='product_bundle_pack.allow_bundle'
    )