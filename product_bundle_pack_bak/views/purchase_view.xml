<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <!-- Add a new tab for Product Packs -->
            <xpath expr="//page[@name='order_lines']" position="after">
                <page string="Product Packs" name="product_packs">
                    <field name="product_pack_ids">
                        <tree>
                            <field name="product_id"/>
                            <field name="qty_uom"/>
                            <field name="price"/>
                            <field name="uom_id"/>
                        </tree>
                    </field>
                </page>
            </xpath>
            <!-- Add a new button to recalculate product pack -->
            <xpath expr="//button[@name='action_view_picking']" position="before">
            <button name="recalculate_product_pack" 
                    string="Recalculate Product Pack" 
                    type="object" 
                    class="btn-secondary" 
                    attrs="{'invisible': [('order_line', '=', False)]}" />
        </xpath>
        </field>
    </record>
</odoo>